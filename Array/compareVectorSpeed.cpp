#include<bits/stdc++.h>
#include<ctime>
using namespace std;
int main()
{
/*
	int r=20;
	int c=20;
	vector<vector<int> > v;
	v.reserve(r*c);
	vector<vector<int> > arr(r,vector<int>(c));
	clock_t start = clock();
	for(int i=0;i<r;i++)
	{
		vector<int> temp;
		temp.reserve(c);
		for(int j=0;j<c;j++)
			temp.push_back(i+j);
		v.push_back(temp);
	}
	cout<<"content:"<<endl;
	for(int i=0;i<r;i++)
	{
		for(int j=0;j<c;j++)
			cout<<v[i][j]<<" ";
		cout<<endl;
	}
	clock_t end = clock() - start;
	clock_t duration1 = end - start;
//	double duration1 = ( (double)(end - start) )/ CLOCKS_PER_SEC;
	cout<<"Methods-1: time taken is:"<<duration1<<" clocks"<<endl;

	start = clock();
	for(int i=0;i<r;i++)
	{
		for(int j=0;j<c;j++)
			arr[i][j] = (i+j);
	}
	cout<<"contents 2"<<endl;
	for(int i=0;i<r;i++)
	{
		for(int j=0;j<c;j++)
			cout<<v[i][j]<<" ";
		cout<<endl;
	}

	end = clock() - start;
	clock_t duration2 = end - start;
//	double duration2 = ( (double)(end - start) )/ CLOCKS_PER_SEC;
	cout<<"Methods-2: time taken is:"<<duration2<<" clocks"<<endl;
	if(duration1 < duration2)
		cout<<"Method 1 is faster"<<endl;
	else
		cout<<"Method 2 is faster"<<endl;
*/
}
